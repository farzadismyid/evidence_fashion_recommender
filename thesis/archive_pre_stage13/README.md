# Pre-Stage 13 Thesis Archive

This directory preserves the thesis chapters as they existed at Git commit `874cb6a`
before the Stage 13 final-release rewrite.

- `contents/thesis/` contains the original Markdown sources and DOCX chapter files.
- `pre_stage13_thesis_chapters.zip` is an unmodified Git archive of the same directory.

These are historical drafts. The current canonical thesis sources remain the Markdown and
DOCX files directly under `thesis/`.
