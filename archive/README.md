# Archived notebook experiment

`notebook_original.ipynb` is the preserved historical implementation and first full
experimental run. `original_notebook_outputs/` contains its generated artifacts.

These files are retained for traceability. New experiments must use the modular package
and write to the repository's new `outputs/` directory.

The original output directory is intentionally ignored by Git because it contains large
embeddings and generated artifacts.

